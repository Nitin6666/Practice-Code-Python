import os 
 
#The directory which file names was to be prints

directory='/'

#The contents which are extracted from the directory was to be selected

contents=os.listdir(directory)

print(contents)#Printing the contents of the directory