#A tuple is such like list bust is mutable in nature means that the item will not change after it is made no method is used to change it when using such methods they will return a new tuple but the original was not changed 
#Important point a tuple that have only one element is always made with comma if the comma will not attach to it it is such a integer data type
a=(1,)
print(len(a))
print(type(a))